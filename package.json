{
  "name": "arcadia-book",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api,bot -c blue,green,yellow \"npm run dev --prefix web\" \"npm run dev --prefix api\" \"npm run dev --prefix bot\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
