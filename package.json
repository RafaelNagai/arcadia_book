{
  "name": "arcadia-book",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev --prefix web\" \"npm run dev --prefix api\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
